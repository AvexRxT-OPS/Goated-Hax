local a = game:GetService("Players")
local b = a.LocalPlayer
local c = b.Character or b.CharacterAdded:Wait()
local d = c:WaitForChild("Humanoid")
local e = game:GetService("UserInputService")
local f = game:GetService("StarterGui")

local function g(h)
    if d then
        d.WalkSpeed = h
        print("Speed set to " .. h)

        local i = Instance.new("TextLabel")
        i.Size = UDim2.new(0, 200, 0, 50)
        i.Position = UDim2.new(1, -210, 1, -60)
        i.BackgroundColor3 = Color3.new(0, 0, 0)
        i.BackgroundTransparency = 0.5
        i.Text = "Speed: " .. h
        i.TextColor3 = Color3.new(1, 1, 1)
        i.TextSize = 24
        i.Parent = b.PlayerGui

        wait(3)
        i:Destroy()
    else
        warn("Humanoid not found!")
    end
end

e.InputBegan:Connect(function(j, k)
    if k then return end

    if j.UserInputType == Enum.UserInputType.Keyboard then
        if j.KeyCode == Enum.KeyCode.V then
            g(50)
        elseif j.KeyCode == Enum.KeyCode.B then
            g(100)
        elseif j.KeyCode == Enum.KeyCode.T then
            g(200)
        end
    end
end)

f:SetCore("SendNotification", {
    Title = "Avex Speed",
    Text = "Loaded",
    Duration = 5
})

-- Redundant Code
local function l()
    local m = 10
    local n = 20
    local o = m + n
    return o
end

local function p()
    local q = "Obfuscation"
    local r = "Complete"
    local s = q .. " " .. r
    print(s)
end

l()
p()
